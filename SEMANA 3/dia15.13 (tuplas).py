# Lista (Mutable - Se puede cambiar)
usuarios = ["Ana", "Beto"]

# Tupla (Inmutable - NO se puede cambiar)
coordenadas_base = (10, 20)

mi_tupla = ("A", "B", "C")

print(mi_tupla[0]) # Imprime "A". (Leer sí se puede)

# Intentar cambiar algo:
mi_tupla[0] = "Z"